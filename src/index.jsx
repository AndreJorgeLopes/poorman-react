//virtual dom (createElement, render)
//hooks
//concurrent mode
import createElement from "./createElement";

const React = {
    createElement
};

const Index = () => {
    return (
        <div name="andre">
            <div about="a">index</div>
        </div>
    );
}

<Index/>